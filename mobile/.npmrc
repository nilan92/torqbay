# React 19.2.3 is newer than the peer ranges declared by react-test-renderer
# and parts of expo-router's web stack, so a strict peer check fails on every
# install. Nothing here is actually incompatible — the suite passes — so allow
# the legacy resolution rather than requiring the flag by hand each time.
legacy-peer-deps=true
