# Node / Expo
node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
*.db

# Env / secrets
.env
.env.*
!.env.example
# mobile/.env holds only the public API host (EXPO_PUBLIC_ vars are inlined at
# build time and are not secret) and must be committed for the app to build.
!mobile/.env

# OS
.DS_Store

# Editor
.vscode/
.idea/

# Claude Code worktrees / local session state
.claude/

# Expo / React Native
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/ios/
mobile/android/
mobile/*.log
